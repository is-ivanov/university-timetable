version: "3.9"

services:
  postgres:
    image: postgres:13.1-alpine
    environment:
      POSTGRES_DB: "university_hibernate"
      POSTGRES_USER: "postgres"
      POSTGRES_PASSWORD: "1234"
      PGDATA: "/var/lib/postgresql/data/pgdata"
    volumes:
      - .:/var/lib/postgresql/data
    ports:
      - "5432:5432"