#queries from CourseDao.class
Course.getAll = SELECT c FROM Course c ORDER BY c.name
Course.getAllSortedPaginated = SELECT c FROM Course c ORDER BY %s %s
Course.deleteById = DELETE FROM Course c WHERE c.id = :id
Course.countAll = SELECT count(c.id) FROM Course c

##queries from RoomDao.class
#room.add = INSERT INTO rooms(building, room_number) VALUES (?, ?)
#room.getById = SELECT room_id, building, room_number FROM rooms WHERE room_id = ?
#room.getAll = SELECT room_id, building, room_number FROM rooms ORDER BY building, room_number
#room.getAllSortedPaginated = SELECT room_id, building, room_number \
#                             FROM rooms \
#                             ORDER BY %s %s \
#                             OFFSET %d ROWS FETCH NEXT %d ROWS ONLY
#room.update = UPDATE rooms SET building = ?, room_number = ? WHERE room_id = ?
#room.delete = DELETE FROM rooms WHERE room_id = ?
#room.countAll = SELECT count(*) FROM rooms
#room.getFreeRoomsOnLessonTime = SELECT r.room_id, r.building, r.room_number \
#                                FROM rooms r \
#                                WHERE r.room_id NOT IN \
#                                    ( \
#                                        SELECT r2.room_id \
#                                        FROM rooms r2 \
#                                            LEFT JOIN lessons l ON r2.room_id = l.room_id \
#                                        WHERE l.time_end >= ? \
#                                          AND l.time_start <= ? \
#                                    ) \
#                                ORDER BY r.building, r.room_number

#queries from FacultyDao.class
Faculty.getAll = SELECT f FROM Faculty f
Faculty.getAllSortedPaginated = SELECT f FROM Faculty f ORDER BY %s %s
Faculty.getAllSortedByNameAsc = SELECT f FROM Faculty f ORDER BY f.name ASC
Faculty.deleteById = DELETE FROM Faculty f WHERE f.id = :id
Faculty.countAll = SELECT count(f.id) FROM Faculty f

#queries from GroupDao.class
Group.getAll = SELECT g FROM Group g \
               ORDER BY g.name
Group.getAllActive = SELECT g FROM Group g \
                     WHERE g.active = TRUE \
                     ORDER BY g.name
Group.getAllByFaculty = SELECT g FROM Group g \
                        WHERE g.faculty.id = :facultyId
Group.deleteById = DELETE FROM Group g \
                   WHERE g.id = :id
Group.getFreeGroupsOnLessonTime = SELECT DISTINCT g \
                                  FROM Group g \
                                    LEFT JOIN Student s ON s.group = g \
                                  WHERE g.active = TRUE \
                                    AND s.active = TRUE \
                                    AND s.id NOT IN \
                                  ( \
                                    SELECT s2.id \
                                    FROM Student s2 \
                                        JOIN s2.lessons l \
                                    WHERE s2.active = TRUE \
                                      AND l.timeEnd >= :time_start \
                                      AND l.timeStart <= :time_end \
                                  ) \
                                  ORDER BY g.name
Group.getFreeGroupsByFacultyOnLessonTime = SELECT DISTINCT g \
                                           FROM Group g \
                                               LEFT JOIN Student s ON s.group = g \
                                           WHERE g.active = TRUE \
                                             AND g.faculty.id = :facultyId \
                                             AND s.active = TRUE \
                                             AND s.id NOT IN \
                                               (\
                                                   SELECT s2.id \
                                                   FROM Student s2 \
                                                       JOIN s2.lessons l \
                                                   WHERE s2.active = TRUE \
                                                     AND l.timeEnd >= :time_start \
                                                     AND l.timeStart <= :time_end \
                                               ) \
                                           ORDER BY g.name
##queries from StudentDao.class
#student.add = INSERT INTO students(student_first_name, student_last_name, student_patronymic, student_active, group_id) VALUES (?, ?, ?, ?, ?)
#student.getById = SELECT s.student_id, s.student_first_name, s.student_last_name, s.student_patronymic, s.student_active, \
#                         s.group_id, g.group_name, g.group_active, g.faculty_id, \
#                         f.faculty_name \
#                FROM students s \
#                    INNER JOIN groups g ON s.group_id = g.group_id \
#                    INNER JOIN faculties f ON g.faculty_id = f.faculty_id \
#                WHERE s.student_id = ?
#student.getAll = SELECT s.student_id, s.student_first_name, s.student_last_name, s.student_patronymic, s.student_active, s.group_id, \
#                        g.group_name, g.group_active, g.faculty_id, \
#                        f.faculty_name \
#                 FROM students s \
#                    INNER JOIN groups g ON s.group_id = g.group_id \
#                    INNER JOIN faculties f ON g.faculty_id = f.faculty_id \
#                 ORDER BY student_last_name, student_first_name, student_patronymic
#student.getAllActive = SELECT s.student_id, \
#                              s.student_first_name, \
#                              s.student_last_name, \
#                              s.student_patronymic, \
#                              s.student_active, \
#                              s.group_id, \
#                              g.group_name, \
#                              g.group_active, \
#                              g.faculty_id, \
#                              f.faculty_name \
#                       FROM students s \
#                                INNER JOIN groups g ON s.group_id = g.group_id \
#                                INNER JOIN faculties f ON g.faculty_id = f.faculty_id \
#                       WHERE s.student_active = TRUE \
#                       ORDER BY s.student_last_name, s.student_first_name
#student.update = UPDATE students SET student_first_name = ?, student_last_name = ?, student_patronymic = ?, student_active = ?, group_id = ? WHERE student_id = ?
#student.delete = DELETE FROM students WHERE student_id = ?
#student.getStudentsByLesson = SELECT sl.student_id, s.student_first_name, s.student_last_name, s.student_patronymic, s.student_active, \
#                                    g.group_id, g.group_name, g.group_active, \
#                                    f.faculty_id, f.faculty_name \
#                            FROM students_lessons sl \
#                                INNER JOIN students s ON sl.student_id = s.student_id \
#                                INNER JOIN groups g ON s.group_id = g.group_id \
#                                INNER JOIN faculties f ON g.faculty_id = f.faculty_id \
#                            WHERE sl.lesson_id = ?
#student.getStudentsByGroup = SELECT s.student_id, s.student_first_name, s.student_last_name, s.student_patronymic, s.student_active, s.group_id, \
#	                       g.group_name, g.group_active, g.faculty_id, \
#	                       f.faculty_name \
#	                       FROM students s \
#	                       INNER JOIN groups g ON s.group_id = g.group_id \
#	                       INNER JOIN faculties f ON g.faculty_id = f.faculty_id \
#	                       WHERE g.group_id = ?
#student.getStudentsByFaculty = SELECT s.student_id, s.student_first_name, s.student_last_name, s.student_patronymic, s.student_active, s.group_id, \
#	                         g.group_name, g.group_active, g.faculty_id, \
#	                         f.faculty_name \
#	                         FROM students s \
#	                         INNER JOIN groups g ON s.group_id = g.group_id \
#	                         INNER JOIN faculties f ON g.faculty_id = f.faculty_id \
#	                         WHERE f.faculty_id = ?
#student.getFreeStudentsFromGroupOnLessonTime = SELECT s.student_id, \
#                                                      s.student_first_name, \
#                                                      s.student_patronymic, \
#                                                      s.student_last_name, \
#                                                      s.student_active, \
#                                                      g.group_id, \
#                                                      g.group_name, \
#                                                      g.group_active, \
#                                                      f.faculty_id, \
#                                                      f.faculty_name \
#                                               FROM students s \
#                                                    LEFT JOIN groups g ON g.group_id = s.group_id \
#                                                    LEFT JOIN faculties f ON f.faculty_id = g.faculty_id \
#                                               WHERE g.group_id = ? \
#                                                 AND group_active = TRUE \
#                                                 AND student_active = TRUE \
#                                                 AND s.student_id NOT IN \
#                                                    ( \
#                                                        SELECT s2.student_id \
#                                                        FROM students s2 \
#                                                            LEFT JOIN students_lessons sl ON s2.student_id = sl.student_id \
#                                                            LEFT JOIN lessons l ON sl.lesson_id = l.lesson_id \
#                                                        WHERE l.time_end >= ? \
#                                                          AND l.time_start <= ? \
#                                                    ) \
#                                               ORDER BY s.student_last_name, s.student_first_name

#queries from DepartmentDao.class
Department.getAll = SELECT d FROM Department d \
                    ORDER BY d.name
Department.getAllByFacultyId = SELECT d FROM Department d \
                                WHERE d.faculty.id = :facultyId \
                                ORDER BY d.name
Department.deleteById = DELETE FROM Department d \
                        WHERE d.id = :id

##queries from TeacherDao.class
#teacher.add = INSERT INTO teachers(teacher_first_name, teacher_last_name, teacher_patronymic, teacher_active, department_id) VALUES (?, ?, ?, ?, ?)
#teacher.getById = SELECT t.teacher_id, t.teacher_first_name, t.teacher_last_name, t.teacher_patronymic, t.teacher_active, t.department_id, \
#                d.department_name, d.faculty_id, \
#                f.faculty_name \
#                FROM teachers t \
#                INNER JOIN departments d ON t.department_id = d.department_id \
#                INNER JOIN faculties f ON d.faculty_id = f.faculty_id \
#                WHERE t.teacher_id = ?
#teacher.getAll = SELECT t.teacher_id, t.teacher_first_name, t.teacher_last_name, t.teacher_patronymic, t.teacher_active, t.department_id, \
#                        d.department_name, d.faculty_id, \
#                        f.faculty_name \
#                 FROM teachers t \
#                     INNER JOIN departments d ON t.department_id = d.department_id \
#                     INNER JOIN faculties f ON d.faculty_id = f.faculty_id \
#                 ORDER BY teacher_last_name, teacher_first_name
#teacher.update = UPDATE teachers SET teacher_first_name = ?, teacher_last_name = ?, teacher_patronymic = ?, teacher_active = ?, department_id = ? WHERE teacher_id = ?
#teacher.delete = DELETE FROM teachers WHERE teacher_id = ?
#teacher.getTeachersByDepartment = SELECT t.teacher_id, t.teacher_first_name, t.teacher_last_name, t.teacher_patronymic, t.teacher_active, t.department_id, \
#                d.department_name, d.faculty_id, \
#                f.faculty_name \
#                FROM teachers t \
#                INNER JOIN departments d ON t.department_id = d.department_id \
#                INNER JOIN faculties f ON d.faculty_id = f.faculty_id \
#                WHERE t.department_id = ?
#teacher.getTeachersByFaculty = SELECT t.teacher_id, t.teacher_first_name, t.teacher_last_name, t.teacher_patronymic, t.teacher_active, t.department_id, \
#                d.department_name, d.faculty_id, \
#                f.faculty_name \
#                FROM teachers t \
#                INNER JOIN departments d ON t.department_id = d.department_id \
#                INNER JOIN faculties f ON d.faculty_id = f.faculty_id \
#                WHERE d.faculty_id = ?
#teacher.getFreeTeachersOnLessonTime = SELECT t.teacher_id, \
#                                             t.teacher_first_name, \
#                                             t.teacher_last_name, \
#                                             t.teacher_patronymic, \
#                                             t.teacher_active, \
#                                             d.department_id, \
#                                             d.department_name, \
#                                             f.faculty_id, \
#                                             f.faculty_name \
#                                      FROM teachers t \
#                                        LEFT JOIN departments d ON d.department_id = t.department_id \
#                                        LEFT JOIN faculties f ON f.faculty_id = d.faculty_id \
#                                      WHERE t.teacher_active = TRUE \
#                                        AND t.teacher_id NOT IN \
#                                            ( \
#                                                SELECT t2.teacher_id \
#                                                FROM teachers t2 \
#                                                  LEFT JOIN lessons l ON t2.teacher_id = l.teacher_id \
#                                                WHERE l.time_end >= ? \
#                                                  AND l.time_start <= ? \
#                                            ) \
#                                      ORDER BY t.teacher_last_name, t.teacher_first_name
##queries from LessonDao.class
#lesson.add = INSERT INTO lessons (teacher_id, course_id, room_id, time_start, time_end) VALUES (?, ?, ?, ?, ?)
#lesson.getById = SELECT l.lesson_id, l.teacher_id, l.course_id, l.room_id, l.time_start, l.time_end, \
#               t.teacher_first_name, t.teacher_last_name, t.teacher_patronymic, t.teacher_active, t.department_id, \
#               d.department_name, d.faculty_id AS teacher_faculty_id, \
#               f.faculty_name AS teacher_faculty_name, \
#               c.course_name, \
#               r.building, r.room_number,\
#               sl.student_id,\
#               s.student_first_name, s.student_last_name, s.student_patronymic, s.student_active, s.group_id, \
#               g.group_name, g.group_active, g.faculty_id, \
#               f2.faculty_name \
#               FROM lessons l \
#               LEFT JOIN teachers t ON l.teacher_id = t.teacher_id \
#               LEFT JOIN departments d ON t.department_id = d.department_id \
#               LEFT JOIN faculties f ON d.faculty_id = f.faculty_id \
#               LEFT JOIN courses c ON l.course_id = c.course_id \
#               LEFT JOIN rooms r ON l.room_id = r.room_id \
#               LEFT JOIN students_lessons sl ON l.lesson_id = sl.lesson_id \
#               LEFT JOIN students s ON sl.student_id = s.student_id \
#               LEFT JOIN groups g ON s.group_id = g.group_id \
#               LEFT JOIN faculties f2 ON g.faculty_id = f2.faculty_id \
#               WHERE l.lesson_id = ?
#lesson.getAll = SELECT l.lesson_id, l.teacher_id, l.course_id, l.room_id, l.time_start, l.time_end, \
#              t.teacher_first_name, t.teacher_last_name, t.teacher_patronymic, t.teacher_active, t.department_id, \
#              d.department_name, d.faculty_id AS teacher_faculty_id, \
#              f.faculty_name AS teacher_faculty_name, \
#              c.course_name, \
#              r.building, r.room_number,\
#              sl.student_id,\
#              s.student_first_name, s.student_last_name, s.student_patronymic, s.student_active, s.group_id, \
#              g.group_name, g.group_active, g.faculty_id, \
#              f2.faculty_name \
#              FROM lessons l \
#              LEFT JOIN teachers t ON l.teacher_id = t.teacher_id \
#              LEFT JOIN departments d ON t.department_id = d.department_id \
#              LEFT JOIN faculties f ON d.faculty_id = f.faculty_id \
#              LEFT JOIN courses c ON l.course_id = c.course_id \
#              LEFT JOIN rooms r ON l.room_id = r.room_id \
#              LEFT JOIN students_lessons sl ON l.lesson_id = sl.lesson_id \
#              LEFT JOIN students s ON sl.student_id = s.student_id \
#              LEFT JOIN groups g ON s.group_id = g.group_id \
#              LEFT JOIN faculties f2 ON g.faculty_id = f2.faculty_id
#lesson.update = UPDATE lessons SET teacher_id = ?, course_id = ?, room_id = ?, time_start = ?, time_end = ? WHERE lesson_id = ?
#lesson.delete = DELETE FROM lessons WHERE lesson_id = ?
#lesson.addStudentToLesson = INSERT INTO students_lessons (lesson_id, student_id) VALUES (?, ?)
#lesson.deleteAllStudentsFromLesson = DELETE FROM students_lessons WHERE lesson_id = ?
#lesson.deleteStudentFromLesson = DELETE FROM students_lessons WHERE lesson_id = ? AND student_id = ?
#lesson.getAllForTeacher = SELECT l.lesson_id, l.teacher_id, l.course_id, l.room_id, l.time_start, l.time_end, \
#       t.teacher_first_name, t.teacher_last_name, t.teacher_patronymic, t.teacher_active, t.department_id, \
#       d.department_name, d.faculty_id AS teacher_faculty_id, \
#       f.faculty_name AS teacher_faculty_name, \
#       c.course_name, \
#       r.building, r.room_number,\
#       sl.student_id,\
#       s.student_first_name, s.student_last_name, s.student_patronymic, s.student_active, s.group_id, \
#       g.group_name, g.group_active, g.faculty_id, \
#       f2.faculty_name \
#       FROM lessons l \
#       LEFT JOIN teachers t ON l.teacher_id = t.teacher_id \
#       LEFT JOIN departments d ON t.department_id = d.department_id \
#       LEFT JOIN faculties f ON d.faculty_id = f.faculty_id \
#       LEFT JOIN courses c ON l.course_id = c.course_id \
#       LEFT JOIN rooms r ON l.room_id = r.room_id \
#       LEFT JOIN students_lessons sl ON l.lesson_id = sl.lesson_id \
#       LEFT JOIN students s ON sl.student_id = s.student_id \
#       LEFT JOIN groups g ON s.group_id = g.group_id \
#       LEFT JOIN faculties f2 ON g.faculty_id = f2.faculty_id \
#       WHERE l.teacher_id = ?
#lesson.getAllForRoom = SELECT l.lesson_id, l.teacher_id, l.course_id, l.room_id, l.time_start, l.time_end, \
#       t.teacher_first_name, t.teacher_last_name, t.teacher_patronymic, t.teacher_active, t.department_id, \
#       d.department_name, d.faculty_id AS teacher_faculty_id, \
#       f.faculty_name AS teacher_faculty_name, \
#       c.course_name, \
#       r.building, r.room_number,\
#       sl.student_id,\
#       s.student_first_name, s.student_last_name, s.student_patronymic, s.student_active, s.group_id, \
#       g.group_name, g.group_active, g.faculty_id, \
#       f2.faculty_name \
#       FROM lessons l \
#       LEFT JOIN teachers t ON l.teacher_id = t.teacher_id \
#       LEFT JOIN departments d ON t.department_id = d.department_id \
#       LEFT JOIN faculties f ON d.faculty_id = f.faculty_id \
#       LEFT JOIN courses c ON l.course_id = c.course_id \
#       LEFT JOIN rooms r ON l.room_id = r.room_id \
#       LEFT JOIN students_lessons sl ON l.lesson_id = sl.lesson_id \
#       LEFT JOIN students s ON sl.student_id = s.student_id \
#       LEFT JOIN groups g ON s.group_id = g.group_id \
#       LEFT JOIN faculties f2 ON g.faculty_id = f2.faculty_id \
#       WHERE l.room_id = ?
#lesson.getAllForStudent = SELECT l.lesson_id, l.teacher_id, l.course_id, l.room_id, l.time_start, l.time_end, \
#       t.teacher_first_name, t.teacher_last_name, t.teacher_patronymic, t.teacher_active, t.department_id, \
#       d.department_name, d.faculty_id AS teacher_faculty_id, \
#       f.faculty_name AS teacher_faculty_name, \
#       c.course_name, \
#       r.building, r.room_number,\
#       sl.student_id,\
#       s.student_first_name, s.student_last_name, s.student_patronymic, s.student_active, s.group_id, \
#       g.group_name, g.group_active, g.faculty_id, \
#       f2.faculty_name \
#       FROM lessons l \
#       LEFT JOIN teachers t ON l.teacher_id = t.teacher_id \
#       LEFT JOIN departments d ON t.department_id = d.department_id \
#       LEFT JOIN faculties f ON d.faculty_id = f.faculty_id \
#       LEFT JOIN courses c ON l.course_id = c.course_id \
#       LEFT JOIN rooms r ON l.room_id = r.room_id \
#       LEFT JOIN students_lessons sl ON l.lesson_id = sl.lesson_id \
#       LEFT JOIN students s ON sl.student_id = s.student_id \
#       LEFT JOIN groups g ON s.group_id = g.group_id \
#       LEFT JOIN faculties f2 ON g.faculty_id = f2.faculty_id \
#       WHERE sl.student_id = ?
#lesson.getAllForStudentForTimePeriod = SELECT l.lesson_id, \
#                                              l.teacher_id, \
#                                              l.course_id, \
#                                              l.room_id, \
#                                              l.time_start, \
#                                              l.time_end, \
#                                              t.teacher_first_name, \
#                                              t.teacher_last_name, \
#                                              t.teacher_patronymic, \
#                                              t.teacher_active, \
#                                              t.department_id, \
#                                              d.department_name, \
#                                              d.faculty_id   AS teacher_faculty_id, \
#                                              f.faculty_name AS teacher_faculty_name, \
#                                              c.course_name, \
#                                              r.building, \
#                                              r.room_number, \
#                                              sl.student_id, \
#                                              s.student_first_name, \
#                                              s.student_last_name, \
#                                              s.student_patronymic, \
#                                              s.student_active, \
#                                              s.group_id, \
#                                              g.group_name, \
#                                              g.group_active, \
#                                              g.faculty_id, \
#                                              f2.faculty_name \
#                                       FROM lessons l \
#                                           INNER JOIN teachers t ON l.teacher_id = t.teacher_id \
#                                           INNER JOIN departments d ON t.department_id = d.department_id \
#                                           INNER JOIN faculties f ON d.faculty_id = f.faculty_id \
#                                           INNER JOIN courses c ON l.course_id = c.course_id \
#                                           INNER JOIN rooms r ON l.room_id = r.room_id \
#                                           INNER JOIN students_lessons sl ON l.lesson_id = sl.lesson_id \
#                                           INNER JOIN students s ON sl.student_id = s.student_id \
#                                           INNER JOIN groups g ON s.group_id = g.group_id \
#                                           INNER JOIN faculties f2 ON g.faculty_id = f2.faculty_id \
#                                       WHERE sl.student_id = ? \
#                                         AND time_start >= ? \
#                                         AND time_end <= ?
#lesson.getAllForTeacherForTimePeriod = SELECT l.lesson_id, \
#                                              l.teacher_id, \
#                                              l.course_id, \
#                                              l.room_id, \
#                                              l.time_start, \
#                                              l.time_end, \
#                                              t.teacher_first_name, \
#                                              t.teacher_last_name, \
#                                              t.teacher_patronymic, \
#                                              t.teacher_active, \
#                                              t.department_id, \
#                                              d.department_name, \
#                                              d.faculty_id   AS teacher_faculty_id, \
#                                              f.faculty_name AS teacher_faculty_name, \
#                                              c.course_name, \
#                                              r.building, \
#                                              r.room_number, \
#                                              sl.student_id, \
#                                              s.student_first_name, \
#                                              s.student_last_name, \
#                                              s.student_patronymic, \
#                                              s.student_active, \
#                                              s.group_id, \
#                                              g.group_name, \
#                                              g.group_active, \
#                                              g.faculty_id, \
#                                              f2.faculty_name \
#                                       FROM lessons l \
#                                           INNER JOIN teachers t ON l.teacher_id = t.teacher_id \
#                                           INNER JOIN departments d ON t.department_id = d.department_id \
#                                           INNER JOIN faculties f ON d.faculty_id = f.faculty_id \
#                                           INNER JOIN courses c ON l.course_id = c.course_id \
#                                           INNER JOIN rooms r ON l.room_id = r.room_id \
#                                           LEFT JOIN students_lessons sl ON l.lesson_id = sl.lesson_id \
#                                           LEFT JOIN students s ON sl.student_id = s.student_id \
#                                           LEFT JOIN groups g ON s.group_id = g.group_id \
#                                           LEFT JOIN faculties f2 ON g.faculty_id = f2.faculty_id \
#                                       WHERE l.teacher_id = ? \
#                                         AND time_start >= ? \
#                                         AND time_end <= ?
#lesson.getAllForRoomForTimePeriod = SELECT l.lesson_id, \
#                                           l.teacher_id, \
#                                           l.course_id, \
#                                           l.room_id, \
#                                           l.time_start, \
#                                           l.time_end, \
#                                           t.teacher_first_name, \
#                                           t.teacher_last_name, \
#                                           t.teacher_patronymic, \
#                                           t.teacher_active, \
#                                           t.department_id, \
#                                           d.department_name, \
#                                           d.faculty_id   AS teacher_faculty_id, \
#                                           f.faculty_name AS teacher_faculty_name, \
#                                           c.course_name, \
#                                           r.building, \
#                                           r.room_number, \
#                                           sl.student_id, \
#                                           s.student_first_name, \
#                                           s.student_last_name, \
#                                           s.student_patronymic, \
#                                           s.student_active, \
#                                           s.group_id, \
#                                           g.group_name, \
#                                           g.group_active, \
#                                           g.faculty_id, \
#                                           f2.faculty_name \
#                                    FROM lessons l \
#                                        INNER JOIN teachers t ON l.teacher_id = t.teacher_id \
#                                        INNER JOIN departments d ON t.department_id = d.department_id \
#                                        INNER JOIN faculties f ON d.faculty_id = f.faculty_id \
#                                        INNER JOIN courses c ON l.course_id = c.course_id \
#                                        INNER JOIN rooms r ON l.room_id = r.room_id \
#                                        LEFT JOIN students_lessons sl ON l.lesson_id = sl.lesson_id \
#                                        LEFT JOIN students s ON sl.student_id = s.student_id \
#                                        LEFT JOIN groups g ON s.group_id = g.group_id \
#                                        LEFT JOIN faculties f2 ON g.faculty_id = f2.faculty_id \
#                                    WHERE l.room_id = ? \
#                                      AND time_start >= ? \
#                                      AND time_end <= ?