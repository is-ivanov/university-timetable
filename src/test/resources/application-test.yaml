spring:
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
        highlight_sql: true
        generate_statistics: true
    database-platform: org.hibernate.dialect.PostgreSQL10Dialect
    show-sql: true
  output:
    ansi:
      enabled: ALWAYS
  liquibase:
    enabled: false